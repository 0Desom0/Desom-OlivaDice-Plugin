- **名称**：`BiliShareInfo`
- **作者**：`Desom-fu`
- **版本**：`1.1.2`
- **兼容版本**：`理论兼容所有 3.0 以上的 OlivOS 版本`
- **平台**：`理论支持全平台`
- **前置插件**：`可选：OlivaDiceCore（用于骰主判定、群开关联动、主从账号支持；未安装时基础功能仍可使用）`

### 更新日志

#### 2026.8.27 v1.1.2

- 修复图片、表情等消息段被当成视频引用解析的问题：发普通图片时约 1.5% 概率会因为文件名里凑巧出现 `BV` 加十位字符而误报一个无关视频
- 按 OlivOS 消息段定义过滤 `[OP:image]`、`[OP:face]`、`[OP:record]`、`[OP:video]`、`[OP:at]`、`[OP:poke]`、`[OP:forward]`、`[OP:node]` 等非文本段，只保留 `text`、`share`、`music`、`xml`、`json` 参与解析
- 非 B 站域名的 URL 不再参与 BV/av 号提取，QQ 图片链接、头像链接和小程序预览图里的随机串不会再被误认
- BV 号匹配加上前后边界，`abcBV1234567890`、`BV1234567890_thumb.png` 这类不再命中
- 上述过滤对 `[CQ:...]` 格式同样生效

#### 2026.8.27 v1.1.1

- 修复 QQ 小程序卡片偶尔解析成「同一个 UP 主的另一个视频」的问题：标题对不上的候选一律不再作为结果发出
- 新增标题置信度判定，只接受完整标题匹配或被省略号截断的前缀匹配
- 无法确认视频时会回复解析失败原因（未携带 BV 号、找不到同名视频、或 B 站接口被限流），不再静默也不再猜一个错的
- 视频/直播信息获取失败时同样会回复失败提示
- 同一条失败提示 30 秒内不重复发送
- B 站接口返回 412（风控限流）时自动退避重试，不再因为一次抖动就退化成模糊猜测
- 全局控制请求节奏并串行化请求，减少插件把自己打进限流窗口的概率
- UP 主 mid、关键词投稿、标题搜索和视频详情结果加入 5 分钟缓存，重复分享同一视频不再重复请求
- 关键词投稿接口明确回复「没有匹配投稿」时不再多打两次空间投稿接口
- 标题搜索已命中完整匹配时跳过第二个搜索接口
- 调试日志新增限流原因和标题置信度，便于定位解析失败原因

#### 2026.8.12 v1.1.0

- 新增 B 站直播间链接解析，支持展示封面、直播标题、真实房间号/短号、状态、UP 主、分区、公告、标签、关注数、在线人数、开播时间和链接
- 新增本群直播解析开关 `.bili live on/off`，与普通视频的本群开关独立，并受全局开关控制
- 普通视频解析和直播解析现在均默认开启
- 修复 `live.bilibili.com` 直播链接可能被当作普通视频链接解析的问题
- 优化 QQ 小程序标题搜索：OCR 与标题搜索并行，并综合完整标题、UP 主、播放、弹幕和点赞数据选择候选
- 优化同名视频匹配：优先搜索 OCR 识别到的 UP 主投稿，同分候选才随机选择
- 优化短标题和完整符号标题匹配，保留原始标题中的标点符号
- 修复 QQ 图片 URL 参数中的随机 `av数字` 被误识别为 AV 号的问题
- 优化引用消息处理，只解析引用标记之后由用户本次发送的内容

#### 2026.8.6 v1.0.7

- 优化 qqGuildv2 的引用搜索
- 优化匹配结果

#### 2026.8.6 v1.0.6

- 优化标题搜索
- 源码运行时支持 OCR 回退识别

#### 2026.8.3 v1.0.5

- 支持 qqGuildv2 指名

#### v1.0.3

- 支持 qqGuildv2 SDK

#### v1.0.2

- 修复数据存储没有全部初始化的问题

#### v1.0.1

- 修复数据存储的错误路径，并自动迁移至正确路径
- 数据存储读写加锁

### 下载

[upl-file uuid=e879a9d6-e8aa-4436-9f0a-fa7622844b32 size=27kB]bilishareinfo.zip[/upl-file]

### 说明

BiliShareInfo 是一个 B 站分享解析插件，用于在群聊中自动识别并展示哔哩哔哩视频和直播间信息。

#### 主要功能

- 自动识别群聊中的 B 站视频链接
- 支持识别 BV 号、AV 号
- 支持识别 b23.tv 和 bili2233.cn 短链接
- 支持识别 QQ 小程序形式的哔哩哔哩分享
- 支持识别 `live.bilibili.com` 直播间链接
- 直播间解析可展示封面、主播、状态、公告、分区、标签、关注、在线人数和开播时间
- 支持一条消息内解析多个 B 站视频或直播间
- 只解析文本、`share`、`music`、`xml`、`json` 消息段，图片和表情等非文本段不参与解析
- 非 B 站域名的 URL 不参与 BV/av 号提取，避免图片链接里的随机串被误认
- 支持单条/多条结果使用合并转发展示
- 支持群聊单独开启或关闭普通视频解析
- 支持群聊单独开启或关闭直播解析
- 支持全局开启或关闭全部解析
- 支持设置未单独配置群聊的普通视频默认解析状态
- 可选使用预览图 OCR 识别 UP 主和播放/弹幕/点赞数据
- OCR 与标题搜索并行执行，对最多 20 个候选逐条评分
- 同名标题优先按 UP 主和统计数据核验，评分并列时随机选择
- 标题对不上的候选不会作为解析结果，宁可不回复也不发错误视频
- 无法确认视频或信息获取失败时会回复解析失败原因，同一提示 30 秒内不重复
- B 站接口被风控限流（412）时自动退避重试，并缓存成功结果减少请求
- 避免将 QQ 图片 URL 查询参数中的随机 `av数字` 误判为 AV 号
- 引用消息只解析用户本次输入的内容，不解析被引用消息中的卡片或链接

#### 指令

- `.bili help` 查看帮助
- `.bili on/off` 开启/关闭本群普通视频解析
- `.bili live on/off` 开启/关闭本群直播解析
- `.bili global on/off` 开启/关闭全局解析
- `.bili default on/off` 设置未配置群聊的普通视频默认解析状态
- `.bili singleforward on/off` 设置单条结果是否使用合并转发
- `.bili multiforward on/off` 设置多条结果是否使用合并转发
- `.bili ocr on/off` 开启/关闭预览图 OCR 增强匹配
- `.bili debug on/off` 开启/关闭解析调试日志

#### 默认状态

- 全局解析默认开启
- 未单独配置群聊时，普通视频解析默认开启
- 未单独配置群聊时，直播解析默认开启
- 本群普通视频解析和本群直播解析彼此独立
- 关闭全局解析后，普通视频和直播解析都会停止
- 已经通过指令显式设置过的群会保留自己的开关状态

#### 可选依赖

预览图增强匹配使用轻量的 `rapidocr_onnxruntime`。未安装时会自动跳过 OCR，并回退到标题搜索。

OCR 依赖仅在源码运行环境中安装后生效：

```text
pip install rapidocr_onnxruntime "numpy<2.0" "opencv-python<5"
```

OCR 只在卡片带有 QQ 预览图时运行，不影响普通 BV/AV 链接和直播间链接解析。OCR 与标题搜索结果不一致时，插件会综合比较候选视频的完整标题、UP 主、播放、弹幕和点赞数据。识别出 UP 主后，会优先通过 B 站的 UP 主关键词投稿接口查找视频，并对同名投稿补充详情后再评分。

小程序卡片里没有 BV/av 号，标题是唯一可靠的判定依据。因此候选标题与卡片标题既不完整一致、也不构成省略号截断前缀时，插件不会退回“同一个 UP 主的其它视频”这种猜测结果，而是回复一条解析失败提示，说明是没带 BV 号、找不到同名视频，还是 B 站接口被限流。B 站公开接口按 IP 限流，遇到 412 时插件会退避重试，成功结果缓存 5 分钟；同一条失败提示 30 秒内不会重复发送。

直播解析使用 B 站直播房间公开接口。本群直播开关独立于普通视频的本群开关，但全局开关关闭时不会触发。

插件只对文本内容做识别。OlivOS 会把图片、表情、语音、视频、at、戳一戳、合并转发等还原成 `[OP:类型,键=值]` 消息段（`[CQ:...]` 同理），这些段里的 `file`／`url` 是一长串随机字符，恰好出现 `BV` 加十位字符的概率约 1.5%，因此解析前会先把它们剔除，只保留 `text`、`share`、`music`、`xml`、`json` 五类可能携带 B 站分享的段。同时非 B 站域名的 URL 不参与 BV／av 号提取，QQ 图片链接、头像链接和小程序预览图里的随机串不会再被误认成视频。

#### 权限说明

- 本群普通视频开关可由群主、管理员、骰主操作
- 本群直播开关可由群主、管理员、骰主操作
- 全局开关、默认开关、合并转发、OCR 和调试日志设置仅骰主可操作

### 截图示例

![Image description](https://forum.olivos.run/assets/files/2026-07-07/1783405837-837341-image.png)
![Image description](https://forum.olivos.run/assets/files/2026-07-07/1783405855-534678-image.png)
![Image description](https://forum.olivos.run/assets/files/2026-07-07/1783405870-880902-image.png)
![Image description](https://forum.olivos.run/assets/files/2026-07-07/1783405878-599601-image.png)
